#toolbar_area
  #toolbar
    #logo
      = link_to "ShopVibe", root_url, :class => :logo
    #login_area
      - if current_user
        = link_to current_user.full_name, profile_url(current_user)
      