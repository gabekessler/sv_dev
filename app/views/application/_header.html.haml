%header#header
  - if current_user
    #user_nav
      = link_to (image_tag current_user.profile.avatar.url(:square)), profile_url(current_user)
      = link_to current_user.full_name, profile_url(current_user)
  %h1= yield :heading
  #search
    search