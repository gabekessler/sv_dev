%header#header
  - if current_user
    = link_to current_user.email, profile_url(current_user.profile)
    %br/
    = link_to "Logout", destroy_user_session_url, :method => :delete
  - else
    = link_to "Login", new_user_session_url
  %h1 Shop Vibe